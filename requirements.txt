# Runtime dependencies for the Streamlit Cloud deploy.
# The app (main.py, pages/, model/) imports only these third-party packages.
# Heavy/build-fragile extras used by offline scripts (nfl_data_py, scipy,
# scikit-learn, psycopg2, etc.) are intentionally excluded so the cloud build
# stays reproducible on modern Python — install them locally when running
# the data/back-test scripts.
# >=1.35 for st.dataframe(on_select=...), which powers the draft simulator's
# click-a-row-to-draft board (see 8_Draft_Simulator.py).
streamlit>=1.35.0
pandas>=2.0
numpy>=1.26
plotly>=5.0
# Player Comparison talks to a local Ollama server over plain HTTP. Streamlit
# already pulls this in; it's listed because utils/ollama_chat.py imports it.
requests>=2.31
